---
const { value, label } = Astro.props as { value: number; label: string };
---

<span class="rating" aria-label={`${label}: ${value}/5`}>
  <span>{label}</span>
  <strong>{"●".repeat(value)}{"○".repeat(5 - value)}</strong>
</span>
