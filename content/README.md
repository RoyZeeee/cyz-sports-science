# Public Content Staging

This folder is reserved for future Markdown content collections. The current first version keeps public site copy in `src/data/content.ts` so FAQ, article, evidence, and topic pages can share one typed data source.
